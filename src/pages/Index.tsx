// Re-export a placeholder so the existing Index route stays compatible.
// The real landing page lives at src/pages/Landing.tsx and is wired in App.tsx.
import Landing from './Landing';
export default Landing;
